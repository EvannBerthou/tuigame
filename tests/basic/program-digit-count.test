1
2
3
4
---
FUNC DIGITS(n);
    IF n == 0;
        RETURN 1;
    END

    count = 0;

    WHILE n > 0;
        n = n / 10;
        count = count + 1;
    END

    RETURN count;
END

PRINTN(DIGITS(7));
PRINTN(DIGITS(42));
PRINTN(DIGITS(999));
PRINTN(DIGITS(1000));
